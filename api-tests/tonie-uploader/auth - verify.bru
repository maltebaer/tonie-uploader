meta {
  name: auth - verify
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth
  body: json
  auth: none
}

body:json {
  {
    "appPassword": "{{appPassword}}",
    "action": "verify"
  }
}
