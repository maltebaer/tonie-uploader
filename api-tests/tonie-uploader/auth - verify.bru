meta {
  name: auth - verify
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/auth
  body: json
  auth: none
}

body:json {
  {
    "appPassword": "{{appPassword}}",
    "action": "verify"
  }
}
